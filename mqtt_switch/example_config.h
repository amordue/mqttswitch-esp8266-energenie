const char* ssid = "WIFI NETWORK NAME";
const char* password = "WIFI NETWORK PASSWORD";
const char* mqtt_server = "MQTT SERVER IP OR HOSTNAME";
const int mqtt_port = 1883; // This is the default port for mqtt
